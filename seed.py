#!/usr/bin/python
import socket
import random

def testSeed(source):
  random.seed(source)
  val = random.randint(0,6)
  print source,' has seed value of: ',val

host['list','of','hostnames']

for host in hosts:
  testSeed(host)
